1️⃣ What is the difference between var, let, and const?


var= var is a function scoped. it can be redeclared and reassigned. Because of hoisting and scope issues,
const = const is block scoped. It can be reassigned but cannot be redeclared in the same scope.
let = let is also block scoped.it cannot be reassign after the value is assigne.


2️⃣ What is the spread operator (...)?

It is one kind of sallow copy. we can marge multiple array or object and also can pass multiple value into function.


3️⃣ What is the difference between map(), filter(), and forEach()?

map() = it creates a new array and transform all element from an original array.its very powerful.
fillter()= we can filter any value with condition from an array and array.

forEach() = when we need get any value from an object or array e can use forEach().it does not return a new array.

4️⃣ What is an arrow function?
arrow function is a shrot version for function .it does not have it's own this function. it is easy to use and control with clean code write.

5️⃣ What are template literals?

Templete literals is very powerfull in js . we can acces any js function into html code and aslo write mutiline string .it embedding javascript expression  with ${}.